package main

func main() {
	// ?
	// ch1 := make(chan int)
	// ch2 := make(chan int)

	// go func() {
	// 	time.Sleep(2 * time.Second)
	// 	ch1 <- 1 // 发送数据到ch1
	// }()

	// select {
	// case <-ch1: // 等待ch1的数据
	// 	println("Received from ch1")
	// case <-ch2: // 等待ch2的数据
	// 	println("Received from ch2")
	// }
	// // 没有 default 分支，且 ch2 永远阻塞导致死锁
}
